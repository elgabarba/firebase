import 'package:flutter/material.dart';

class BtnAuten extends StatelessWidget {
  const BtnAuten({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Texto boton"),
    );
  }
}